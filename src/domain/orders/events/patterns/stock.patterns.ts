export enum StockPatterns {
    RESERVE = "stock.reserve",
    RESERVED = "stock.reserved",
    REJECTED = "stock.rejected",
    COMPENSATE = "stock.compensate",
    COMPENSATED = "stock.compensated",
}

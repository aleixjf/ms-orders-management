export enum OrderStatus {
    PENDING = "pending",
    CONFIRMED = "confirmed",
    CANCELLED = "cancelled",
    SHIPPED = "shipped",
    DELIVERED = "delivered",
    // RETURNED = "returned",
    // REFUNDED = "refunded",
}
